const gandalf = require('gandalf-crud');
export default angular.module('app.login', ['ngMaterial'])
    .controller('LoginController', LoginController)
    .name


LoginController.$inject = ['$mdDialog', '$scope'];
function LoginController($mdDialog,$scope) {

}
